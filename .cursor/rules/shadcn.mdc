---
description: 
globs: *.ts,*.tsx
alwaysApply: false
---
This project uses shadcn. When writing UI components, make sure to use shadcn components wherever possible.

You can install shadcn components using the following command:

```bash
pnpm dlx shadcn@latest add ...
```

The CLI will install any dependencies for you.